const boardStore = {
  namespaced: true,
  state: { boards: [] },
  // getters: {получить данные из стейта (функциями return)},
  // mutations:{только внутри них меняем данные стейта
  // функция (стейт, значение)},
  // actions: {вызов мутации commit('мутация', значение)}
  // деструктуризация
  // obj = {name:a, age:1}
  // const { name } = object;
};
